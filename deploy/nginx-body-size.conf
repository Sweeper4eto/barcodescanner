# Magazin / expire365 — raise upload limit for document photos (base64 JSON).
# Paste into the server { } block of your site nginx config, then:
#   sudo nginx -t && sudo systemctl reload nginx
#
client_max_body_size 12m;
